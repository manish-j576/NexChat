generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model USER {
  id       String @id @default(uuid())
  username String
  password String
}
